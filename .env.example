POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=

# Cloudflare tunnel
TUNNEL_TOKEN=

PGADMIN_USER=

# Optional overrides, default to localhost:5432
# POSTGRES_HOST=localhost
# POSTGRES_PORT=5432

DATABASE_URL=
# Optional direct connection for migrations on hosted databases.
DATABASE_DIRECT_URL=
# Optional runtime overrides. Defaults to 1 connection on Vercel and 10 elsewhere.
# DATABASE_MAX_CONNECTIONS=1
# Enable only when the runtime database endpoint supports prepared statements.
# DATABASE_PREPARED_STATEMENTS=true

SESSION_SECRET=
APP_PASSWORD_HASH_B64=
